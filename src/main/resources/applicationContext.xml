<?xml version="1.0" encoding="UTF-8"?>
<beans  xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:context="http://www.springframework.org/schema/context"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath:musicPlayer.properties"/>

    <bean id="classicalMusicBean"
          class="by.khaletski.practice.ClassicalMusic">
    </bean>
    <bean id="rockMusicBean"
          class="by.khaletski.practice.RockMusic">
    </bean>
    <bean id="jazzMusicBean"
          class="by.khaletski.practice.JazzMusic">
    </bean>

    <bean id="playlistBean"
          class="java.util.ArrayList">
        <constructor-arg>
            <list>
                <ref bean="classicalMusicBean"/>
                <ref bean="rockMusicBean"/>
                <ref bean="jazzMusicBean"/>
            </list>
        </constructor-arg>
    </bean>

    <bean id="musicPlayer"
          class="by.khaletski.practice.MusicPlayer"
          scope="prototype">
        <constructor-arg ref="playlistBean"/>
        <!-- Spring removes the word "set" from setter -->
        <property name="playlist" ref="playlistBean"/>
        <property name="name" value="${musicPlayer.name}"/>
        <property name="volume" value="${musicPlayer.volume}"/>
    </bean>

</beans>
